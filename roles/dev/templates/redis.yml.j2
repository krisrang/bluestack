defaults: &defaults
  uri: <%= uri = URI.parse(ENV['REDIS_URL'] || "redis://127.0.0.1:6379") %>
  host: <%= uri.host %>
  port: <%= uri.port %>
  password: <%= uri.password %>
  db: 0
  cache_db: 1

development:
  <<: *defaults

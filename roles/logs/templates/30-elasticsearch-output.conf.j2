output {
  if [type] == "rails" {
    elasticsearch {
      hosts => ["localhost:{{ logs_es_port }}"]
      sniffing => false
      manage_template => false
      index => "rails-%{+YYYY.MM.dd}"
      document_id => "%{type}-%{uuid}"
    }
  }
}
